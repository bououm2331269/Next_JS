/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        esmExternals: true,  // Activer l'utilisation d'ESM pour les modules externes
      },
};


export default nextConfig;
